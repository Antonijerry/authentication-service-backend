# authentication-service-backend
Production-ready authentication backend built with Java and Spring Boot, featuring secure user registration and login, JWT-based authentication, cookie-based logout, refresh tokens, and OAuth2 social login with Google and GitHub.
